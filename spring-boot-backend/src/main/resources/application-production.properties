# =====================================================
# Production Configuration
# =====================================================

server.port=8986

# Database
spring.datasource.url=jdbc:mysql://10.10.10.13:3306/payglobe?useUnicode=true&characterEncoding=utf8&serverTimezone=Europe/Rome&useLegacyDatetimeCode=false
spring.datasource.username=PGDBUSER
spring.datasource.password=PNeNkar{K1.%D~V
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.show_sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.generate_statistics=false

# JWT
jwt.secret=payglobe-merchant-jwt-secret-2025-change-this-in-production-very-long-key
jwt.expiration=900000
jwt.refresh-expiration=604800000

# Logging
logging.level.root=INFO
logging.level.com.payglobe.merchant=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate.SQL=WARN
logging.file.name=/var/log/merchant-api/application.log
logging.file.max-size=10MB
logging.file.max-history=30

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
